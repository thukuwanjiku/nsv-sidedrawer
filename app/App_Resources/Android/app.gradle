// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.nativescript.nsDrawer"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
